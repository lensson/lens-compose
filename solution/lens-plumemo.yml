version: '3.1'

services:
  lens-plumemo:
    image: lensson/plumemo:latest
    container_name: lens-plumemo
    restart: always
    ports:
      - 9000:9000
    networks:
      - solution_backnet
    volumes:
      - ~/containers/lens-plumemo/files:/files


networks:
  solution_backnet:
    external: true 
